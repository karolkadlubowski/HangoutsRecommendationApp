namespace Library.EventBus
{
    public enum EventType
    {
        UNDEFINED = 0,
        ACCOUNT_PROVIDER_ADDED,
        ACCOUNT_PROVIDER_DELETED,
        FILE_ADDED,
        FILE_DELETED,
        FOLDER_DELETED
    }
}